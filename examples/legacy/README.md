# Historical sketch

`r4850g2_V2.ino` is the original single-PSU experiment, retained for provenance.
It is not a test or a supported build target and still contains the old fixed
current multiplier, RTR data request, and interrupt-context logging. Use the
`mega2560_serial` environment for a maintained screenless controller.
